import React from "react";
import { Text } from "react-native";
import Layout from "../components/Layout";

const User: React.FC = () => {
  return (
    <Layout>
      <Text>User</Text>
    </Layout>
  );
};

export default User;
